# Outbound V1 admission

This feature replaces the placeholder with the first consumer-neutral outbound
webhook implementation. `workflow.toml` remains the state authority. The
feature is not admitted until implementation, evidence, independent
orchestrator review, and one real consumer contract/pin are complete. The first
orchestrator review rejected commit `a5a3b10`; the real-consumer requirement
remains an owner/product blocker while the bounded source findings are repaired.
