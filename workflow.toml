schema_version = 1
project = "gotth-webhooks"
profile = "strict"
active = "dispatcher-lifecycle-repair"

[settings]
worktree_root = "worktrees"
workflow_root = "workflow"
default_branch = "main"

[toolchain]
build = "go build ./..."
test = "go test -mod=readonly -race -cover ./..."
lint = "go vet -mod=readonly ./..."
format = "gofmt -w"
coverage = "go test -mod=readonly -coverprofile=coverage.out ./..."

[coverage]
map = "workflow/COVERAGE.md"
profile = "strict"
block_unresolved_high_risk = true
exception_expiry_days = 14

[feature.outbound-v1-admission]
id = "outbound-v1-admission"
slug = "outbound-v1-admission"
name = "Admit consumer-neutral outbound webhook mechanics"
namespace = "webhooks"
parent = ""
state = "done"
risk = "critical"
assurance = "traceable-tests"
created_at = "2026-09-03T15:37:00Z"
activated_at = "2026-09-03T15:37:00Z"
updated_at = "2026-09-06T04:09:12Z"
completed_at = "2026-09-06T04:09:12Z"
folder = "workflow/features/outbound-v1-admission"
source_path = "pkg/webhooks"
worktree = "/tmp/gotth-webhooks-worktrees/v1-webhooks"
branch = "feature/v1-webhooks"
scope = ["Opaque outbound HTTPS webhook requests", "HMAC-SHA-256 request signing", "Public-network endpoint enforcement", "Bounded retries and receipts", "MIT licensing"]
non_goals = ["Product events or subscriptions", "Inbound webhooks", "Exactly-once claims", "Private endpoints or custom transports", "Durable storage adapter"]
affected_files = ["**"]
invariants = ["Consumers authorize and minimize payloads", "No request processes a redirect or uses an ambient proxy", "A surviving process records every actual attempt before another send", "Secrets, raw endpoints, payloads, and raw errors never enter receipts; delivery fingerprints are sensitive", "No live service or remote repository is mutated"]
acceptance = ["Standalone technical requirements trace to source, tests, and evidence", "Format, vet, race, repeated race, coverage, fuzz, boundary, TLS integration, external compile, performance, and cold review gates pass", "Release and compatibility remain gated by a real-consumer contract, behavioral validation, and exact dependency pin per docs/RELEASING.md"]

[feature.outbound-v1-admission.rollback]
strategy = "revert_local_feature_commits"
notes = "No tag, push, release, deployment, or consumer pin exists"
data_loss_risk = false
requires_migration_down = false

[[feature.outbound-v1-admission.interface]]
name = "Go library API"
kind = "api"
stability = "unstable-unreleased"
change = "new"

[[feature.outbound-v1-admission.verification]]
id = "local"
kind = "unit"
command = "make verify"
required = true
timeout_seconds = 1200
side_effect = "local_write"
network = false
retries = 0

[feature.dispatcher-lifecycle-repair]
id = "dispatcher-lifecycle-repair"
slug = "dispatcher-lifecycle-repair"
name = "Add explicit dispatcher lifecycle"
namespace = "webhooks"
parent = ""
state = "in_progress"
risk = "high"
assurance = "traceable-tests"
created_at = "2026-09-06T17:43:44Z"
activated_at = "2026-09-06T17:43:44Z"
updated_at = "2026-09-06T19:27:57Z"
folder = "workflow/features/dispatcher-lifecycle-repair"
source_path = "pkg/webhooks"
worktree = "/home/linus/.openclaw/workspace/.artifacts/worktrees/gotth-webhooks-dispatcher-lifecycle"
branch = "feature/v1-dispatcher-lifecycle"
scope = ["Shared dispatcher close admission and drain", "Owned transport idle-resource cleanup", "Lifecycle concurrency tests", "Verification evidence"]
non_goals = ["Per-request identity or cancellation tracking", "Consumer generation registry", "Forced cancellation", "Public transport injection", "Remote mutation"]
affected_files = ["README.md", "docs/**", "pkg/webhooks/**", "workflow.toml", "workflow.events.jsonl", "workflow/features/dispatcher-lifecycle-repair/**"]
invariants = ["Close waits for calls admitted before the closed transition", "Calls admitted after close perform no delivery work", "Owned transport cleanup runs exactly once after drain", "No consumer lifecycle policy is invented"]
acceptance = ["ErrClosed and Close are documented public contracts", "Focused lifecycle and concurrency tests cover the issue surface", "Race, repeat, coverage, fuzz, performance, external-consumer, and clean-clone gates pass", "Exact evidence is retained and the feature worktree is clean"]

[feature.dispatcher-lifecycle-repair.rollback]
strategy = "revert_local_feature_commits"
notes = "No push, tag, release, deployment, migration, or consumer pin is part of this repair"
data_loss_risk = false
requires_migration_down = false

[[feature.dispatcher-lifecycle-repair.interface]]
name = "Dispatcher lifecycle API"
kind = "api"
stability = "unstable-unreleased"
change = "additive"

[[feature.dispatcher-lifecycle-repair.verification]]
id = "full"
kind = "unit"
command = "make verify"
required = true
timeout_seconds = 1200
side_effect = "local_write"
network = false
retries = 0
